name: Ga-1
on: 
  push:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
     - name: step1
       run: |
          echo "hello Ga1"
          pwd
          ls -l
          hostnamectl
     - name: step2
       run : |
          wc -l /etc/passwd
          echo $USER 
          sudo useradd -m sheshivardhan
          wc -l /etc/passwd
          head -n 4 /etc/passwd
          
          
                